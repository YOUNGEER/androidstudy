书籍：数据结构和算法（java版本）
https://dsa.cs.tsinghua.edu.cn/~deng/ds/dsaj/dsaj.pdf

java版本的数据结构类型一般有：
栈 Stack : 先入后出，只在头部操作
jVM中，每个线程都会持有一个私有的栈，里面存放着程序的局部变量等信息
里面的元素称为帧，一般存取数据的操作称为 压帧，出帧操作
JVM中有个程序计数器PC，用来记录程序运行的位置

数组 array

队列 Queue ：先入先出 头部出，尾部入

单链表：List 每个节点都是一个对象，包含element和next两个属性，
      element指向数据，next指向下一个节点的地址

双向链表




逻辑层面可以分为：
集合：数据除了同属一个集合，不强调其他的关系
线性：数据之间具有一对一的前后关系，结构中必定存在唯一的首元素和唯一的尾元素
树形：数据元素之间存在一对多的关系
图形：数据元素存在的是多对多的关系

物理存储结构：

